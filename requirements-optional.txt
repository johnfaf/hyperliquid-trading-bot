# ─── Optional Feature Packages ────────────────────────────────
# These are only needed when specific features are enabled in config.
# The bot will start without them — disabled features degrade gracefully.
#
# Install all:     pip install -r requirements-optional.txt
# Install selectively:  pip install xgboost scikit-learn  (for ML regime only)

-r requirements.txt

# ─── ML Regime Forecasting ────────────────────────────────────
# Enable: ENABLE_XGBOOST_FORECASTER=true
xgboost>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# ─── Cross-Venue Hedging (Binance / Bybit) ───────────────────
# Requires API keys: BINANCE_API_KEY, BYBIT_API_KEY
python-binance>=1.0.19
pybit>=5.6.0

# ─── Monte-Carlo Stress Testing ──────────────────────────────
scipy>=1.11.0

# ─── Enhanced Reporting ───────────────────────────────────────
matplotlib>=3.8.0
jinja2>=3.1.0
